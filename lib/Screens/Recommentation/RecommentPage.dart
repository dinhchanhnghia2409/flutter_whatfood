import 'package:flutter/material.dart';

class RecommentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('RecommentPage'),
      ),
    );
  }
}
